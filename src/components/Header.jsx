import React from "react";

const Header = ({ title }) => {
    return (
        <div className="bg-gray-500 h-20">
            <h1>Header {title} page</h1>
        </div>
    );
};

export default Header;
